version: '3.8'
services:
  db:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: sismo_db
    ports:
      - "5432:5432"
    volumes:
      - sismo_db_data:/var/lib/postgresql/data
volumes:
  sismo_db_data: